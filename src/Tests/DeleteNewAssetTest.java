package Tests;

import Tests.AssetTest;

class DeleteNewAssetTest extends AssetTest {

/*    public void setUpBeforeTestMethod(){
        // create a post so we can delete it
        super.setUpBeforeTestMethod();
        testPage.createNewAsset(assetDetails);
    }

    public void testDeleteNewAsset(){
        // test is ready to go!
        testPage.deleteAsset(assetDetails);
        assert !testPage.isAssetPresent(assetDetails);
    }

    // and so on*/
}

